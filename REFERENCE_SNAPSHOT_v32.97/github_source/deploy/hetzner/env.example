# Copy to /opt/neurolab/.env on the VPS. Never commit the real file.
# Paste the same values from Hugging Face Space → Settings → Variables and secrets.

JWT_SECRET=
MFA_ENCRYPTION_KEY=
DB_ENCRYPTION_KEY=
NEUROLAB_ADMIN_EMAIL=
NEUROLAB_ADMIN_PASSWORD=

# Optional — only if you still use Google Drive backups
GOOGLE_SERVICE_ACCOUNT_JSON=
GOOGLE_DRIVE_FOLDER_ID=
